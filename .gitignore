/target
/dist
result
result-*
# Locally-built signed MSIs from tools/codesign/sign-msi.sh.
# Reproducible MSI build (via nix) goes to result/ — this dir
# holds the signed copy, which is non-deterministic by design
# (RFC3161 timestamping).
/out
.direnv
*.swp
*.swo
*~
.DS_Store
output.txt
# Local Claude Code session state (lock files, scratch).
/.claude
# RE / vendor binaries; not source-of-truth code, just analysis fodder.
/research
